import styled from 'styled-components';

const StyledProjectSkillsList = styled.ul`
    list-style-type: none;
    display: flex;
    flex-flow: row wrap;
    justify-content: start;
    align-items: center;
    gap: var(--web-margin-m);
`;

export default StyledProjectSkillsList;
